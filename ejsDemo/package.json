{
  "name": "ejs-demo",
  "version": "1.0.0",
  "description": "Use res.render() to render HTML ()\u001b[Dfrom an ejs file)",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Abudé",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.0.2",
    "express": "^4.17.1"
  }
}
